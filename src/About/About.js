import LinePageHeader from "../PageHeaders/LinePageHeader";

function About() {
    return (
        <section id="about">
          <LinePageHeader reverseDirection={false} headerText="About Me" />
        </section>
      );
}

export default About;